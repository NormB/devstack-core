fastapi==0.141.1
uvicorn[standard]==0.42.0
pydantic-settings==2.14.2
slowapi==0.1.9  # Rate limiting
pybreaker==1.4.1  # Circuit breaker

# HTTP client
httpx==0.28.1

# Database drivers
asyncpg==0.31.0
aiomysql==0.3.2
cryptography>=41.0.0  # Required for MySQL caching_sha2_password auth
motor==3.7.1
pymongo==4.16.0

# Redis and Caching
# fastapi-cache2 0.2.2 is the newest release and its [redis] extra requires
# redis>=4.2.0rc1,<5.0.0; redis 4.6.0 is the last 4.x. Any redis>=5 pin makes
# this file unresolvable (pip: ResolutionImpossible). Dependabot is told to
# leave redis on 4.x in .github/dependabot.yml; do not bump it by hand either.
redis[hiredis]==4.6.0
fastapi-cache2[redis]==0.2.2  # Response caching
# fastapi-cache2's coder module imports starlette.templating, which raises
# ImportError without jinja2 (starlette 1.x). Nothing else pulls it in, so
# without this pin the app cannot be imported from a clean install.
jinja2==3.1.6

# RabbitMQ
aio-pika==10.0.1

# Utilities
python-json-logger==4.0.0

# Observability
prometheus-client==0.24.1

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
pytest-mock==3.15.1
