# Grafana Dashboard Provisioning Configuration

apiVersion: 1

providers:
  - name: 'DevStack Core'
    orgId: 1
    folder: 'DevStack Core'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
